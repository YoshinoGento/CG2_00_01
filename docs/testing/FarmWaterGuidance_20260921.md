# 水分・給水ガイドの検証（2026-09-21）

## 変更範囲

- GrowthSystem::AnalyzeWaterが、既存の水分予測と給水状態から読み取り専用の対処を作る。
- 通常プレイは道具欄上に給水状態と対処を表示。給水状態の行から既存の地形操作を開く。
- Debug Inspectorも同じ対処を使用。給水停止を「給水経路なし」と表示しない。
- 見渡しカメラの下端を論理座標510から440へ変更し、448から始まるガイドを避ける。地形・観察カメラは変更しない。
- 水分・成長・経済・保存形式・給水設定の更新ルールは変更しない。

## 自動確認

- `run_farm_water_guidance_test.cmd`: 成功。最適化・NDEBUG・W4/WXで400ケースと無効入力を検証。
- 2作物、0/1/2/4倍速、給水入/切、利用可能/残水/通水待ち/枯渇/経路なし、水分の適量境界を組み合わせた。
- nullptr、設備マス、未耕作、未植付け、収穫可能、NaN/Inf/範囲外値、予測作物不一致も確認。案内の評価は畑を変更しない。
- `run_farm_runtime_view_test.cmd`: 成功。9対処・5給水状態・停止/表示/編集可否、文字幅、クリック透過防止、既存HUD枠、見渡しカメラの投影境界を検証。
- `run_farm_play_loop_test.cmd`: 成功。既存の育成・品質・販売・大会進行等の回帰確認。
- `run_farm_irrigation_intake_test.cmd`: 成功。1/2/4倍速の給水・水量保存、じょうろ、Undo、古いプレビュー・比較の無効化を回帰確認。
- Debug / Release x64: 最終変更後ともビルド成功。

## 実画面で確認済み

コピー済みの `generated/codex_checks/intake_visible_20260921` のSettingsだけを使用。数値のJSON改変による動作証明は行っていない。

1. Releaseで給水可能な未植付けマスを選択し、「種を植えてから生育を確認」を表示。
2. UIから植え付け、水分100%で「水過多：自動給水を切る」に変わることを確認。
3. 給水状態をクリックして地形操作へ移動。「給水 切」で水分100%を保持したまま、通常画面では給水停止と「追加の水やりを控える」を表示。
4. 初回の案内が畑の手前に重なったため見渡し範囲を修正。再ビルド後、1280x720と最大化1920x1008のクライアントで、畑と案内・操作ボタンの重なりがないことを確認。
5. Debug Inspectorでは「水源への接続範囲：範囲内」「この畑の自動給水：切」を別々に表示。成長後は「収穫できます：追加の水やりは不要」を確認。
6. 確認用農場を保存し、所有するRelease/Debugプロセスを終了。通常セーブ・提出動画・評価文は変更しない。

## 制限と負荷

- 全状態の実画面検証、追従/手動カメラでの常時遮蔽回避、人による30日間プレイ・難易度評価、週次動画撮影は未実施。
- 新規ガイドは選択土マスに最大2ラベル＋2背景Quad。文字Atlasは2560x3120から2560x3296、RGBA基底画像で約1.72MiB増。Texture数・Descriptor/Barrier/Fence所有権は従来どおり。GPU性能測定は未実施。
- Debugコンソールの既存エラー3・警告2表示は本作業では未調査。エンジン全体が無警告という意味ではない。
- 9/17提出用Resourcesが現在の素材へのJunctionだったため、リンクだけをgeneratedへ退避し、独立コピーに変更。今回の更新で変わった提出用PNGはHEADの元画像に戻した。submission全体の追跡差分なし、9/13元セーブSHA256も変更なし。
- ブランチ・HEAD・ステージは変更せず、commit/push/merge/GitHub操作なし。
