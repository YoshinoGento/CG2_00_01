# 3D農業サンドボックスゲーム 全体仕様書 v2

この文書は、今後Codexがこのゲームについて実装・調査・設計を行う際に、ゲーム全体の目的と仕様を理解するための基準資料です。

ただし、この文書だけを見て既存コードを推測して変更しないでください。

実装前には必ず現在のリポジトリを調査し、

- すでに実装されているもの
- 未実装のもの
- 仮実装のもの
- この企画書との違い

を確認してください。

---

# 0. 現在の実装状況（2026-09-05）

この節は企画全体と現在のプロトタイプを区別するための状況記録です。

## 水分による成長効率（2026-09-05実装）

- 土壌水分を乾燥・水不足・適量・水過多に分類する。適量範囲の上下端は適量に含む。不足側・過多側の速度は連続的に変化し、境界で成長速度が飛ばない。
- 仮設定はカブ20～70%、ニンジン35～80%が適量。適量では基準速度100%、満水では25%。乾燥でも成長は止めず、既存の乾燥速度を使用する。FarmRulesの作物別Profileから調整する。
- 共通計算を実更新とEvaluateで使用し、既存の一時Gridプレビューからも同じEvaluateを呼ぶ。表示倍率は水分条件による潜在効率で、未植付け・収穫可能・一時停止中に実際の成長が進む意味ではない。収穫までの秒数は現在速度が続く場合の目安で、水分の未来を積分した保証ではない。
- 畑を選ぶと日本語HUDに水分状態を表示。給水中でも水過多を隠さない。育成中の水過多では「給水を減らして待つ」と表示する。水路・水源は従来の貯水状態表示を維持する。
- 開発モードのInspector「作物の状態」で適量範囲、成長効率、判定理由を確認する。既存の給水予測・水分収支は維持し、給水量が多いことと生育に良いことを区別する。
- 植付け前と収穫可能後の水分消費は従来どおり停止する。じょうろは下記の段階的な給水へ変更済み。適量維持の操作性・自動給水の調整手段は今後のプレイテストで改善する。品質の累積評価・サイズ・通常の土の湛水・虫・コンテストは今回変更していない。
- 検証：境界値、2作物、1/2/4倍速、停止・NaN・無効Crop、実更新と予測の一致、乾燥／適量／過多の成長差、条件改善、一時Gridの本体非変更をCPUテストで確認。既存の灌漑・貯水・プレビュー・ブラシ回帰テストも成功。Debug/Development/Release x64ビルド成功。Debug実画面で給水中の畑の「水過多」、Inspectorの20～70%・効率25%、ゲームモード往復を確認。実画面の植付けから収穫までの速度比較は未検証。既存フォント・文字・Scale・カメラのログは未修正。

## じょうろの段階的な水やり（2026-09-05実装）

- 耕した畑または植付け済みの畑を選び、じょうろを選択してEnterで使用する。従来どおり1回の押下で1回実行し、押しっぱなしの連続給水にはしない。開発モードではInspectorの「2 水やり」→「選択中の農具を適用」でも実行できる。
- 1回で土壌水分を10ポイント追加する（30%→40%）。上限100%で、95%なら5ポイントだけ追加。満水、未耕作、水路・水源には実行しない。水源・用水路の貯水量とは別の値。
- FarmRules.wateringMoistureIncrementで追加量を設定する。0.10が10ポイントに相当し、有効範囲は0.01～1.0。範囲外・非有限値は既定の0.10へ戻す。適量上限で自動停止はしないため、過剰な水やりで水過多になる。
- ゲームHUDはじょうろ選択中に「ENTER 少しずつ水やり」と表示。既存の水分値・乾燥／水不足／適量／水過多で判断する。Inspectorは使用前→使用直後の水分と今回の追加量を表示し、作物の状態には適量範囲・成長効率を表示する。
- 予告は現在選択中の畑への水やり1回だけであり、未来の灌漑・乾燥を含まない。実行直前にもSystemが再評価する。保存形式は変えず、既存のUndo/Redo・未保存管理へ接続する。じょうろの有限残量・購入費は今回追加しない。
- 検証：CPUテストで連続給水、上限、予告一致、不正値・範囲外index、設定値、Undo/Redo、Grid復元後の古い履歴拒否、所持金・種の非変更、2作物の適量／過多を確認。既存灌漑・成長テストとDebug/Development/Release x64ビルド成功。Debug実画面でInspectorから0→10→20%、適量表示、Undo20→10、Redo10→20、ゲームモードの日本語案内を確認。Enter自動入力では変化を確認できず、キーボード実機操作と植付けから収穫までの通し確認は未検証。既存セーブは上書きしていない。

## 完成に向けた進め方（2026-09-05の計画・上記以外は未実装）

完成目標は「自分で地形と用水路を設計し、30日目の巨大作物コンテスト優勝を目指す」。所持金540Gによる現在のクリアは検証用であり、完成版の勝利条件ではない。近距離操作や汎用エンジン機能の追加を、この目的より優先しない。

現行コードとの主な差：FarmGrowthSystemの水分による成長効率は上記のとおり対応済み。FarmCropQualitySystemは収穫時点の成長・水分・高さを評価し、育成中の管理履歴を評価しない。作物はTestCrop（表示はカブ）とCarrotの2種類。FarmDateSystemには60秒の日付と1/2/4倍速があるが、FarmProgressionSystemの終了判定は所持金。水源・用水路の有限貯水は実装済みだが、FarmGridは通常の土のwaterAmountを許可しておらず、低地の土への湛水は別途必要。これはコード照合の結果で、今回の通しプレイ検証ではない。

実装順と完了条件：

1. 水管理の判断を遊びにする。水不足・適量・水過多で成長効率を変える。同じ作物を異なる水分条件で育て、用水路変更で状態が改善することを確認する。現在の2作物で先に検証し、分類の追加と表示だけで完了にしない。
2. 育成履歴を品質・サイズにつなぐ。適量を維持した期間、不足・過多の期間を記録し、成長率と巨大化率を分ける。収穫直前の水やりだけで長期間の悪条件を帳消しにしない。履歴と収穫結果をSave/Load・Undo/Redo・Timelineの対象として設計する。
3. にんじん・トマト・かぼちゃの役割を分ける。種選択・購入・成長・収穫・販売・モデル・表示まで通す。既存TestCropの保存IDを勝手にトマトへ読み替えず、旧セーブの扱いを決める。2×2巨大かぼちゃは保留案。
4. まず10日目の大会まで遊べる区切りを作り、20日・30日へ拡張する。日付イベント、収穫記録、提出対象の選定、結果表示をSystemへ分離。所持金クリアは大会進行への移行と同時に置き換える。売った作物の記録を残すか、対象期間、同点・未収穫・敗北時の扱いは実装前に確定する。早送り・Day Skip・Loadで大会が重複せず、30日終了と再開まで検証する。
5. 虫・農場ランク・利用マス解放を段階追加する。虫の警告日と被害日を区別し、駆除・拡散と評価をつなぐ。マス解放前に描画とCPUコストを計測し、最大Grid化だけで大量の描画オブジェクトを確保しない。
6. 30日通しプレイで価格・育成日数・難度・結果評価を調整する。日本語HUD、操作、モデル・音・レーダーチャートを仕上げる。水位・溢水・低地の湛水は第1段階と区別し、必要な地表水の範囲と既存の貯水ルールを設計してから、最終調整前までに対応する。台風などはコア完成後の候補。

次の実装の設計：

- 目的：水は多ければよいという現行ルールを改め、灌漑の調整に意味を持たせる。Water Amountは地表・水路の水、Moistureは土壌水分、Growth Efficiencyは基準成長速度に対する倍率として区別する。
- 責務：作物別の適正水分範囲・成長倍率はFarmRulesのデータ、状態判定は副作用のない共通計算、状態変更はFarmGrowthSystemへ置く。UIは計算結果の表示と要求通知だけ。Game/Sceneに農業ルールを追加しない。
- 処理順：既存FixedUpdateの灌漑による給水を受けた水分から状態・倍率を求め、成長・水分消費を更新し、その結果を表示へ渡す。Update・成長予測・一時Gridのプレビューで同じ判定を使う。将来の品質・サイズを未実装の段階で確定値として予告しない。
- HUD：選択作物の「水不足／適量／水過多」と影響を短い日本語で表示。色だけに依存せず文字や記号を併用。ImGui：現在水分、適正範囲、成長倍率、給水量・消費量、判定理由を表示。重複した大型パネルを増やさない。
- 安全性・検証：範囲境界、NaN、無効Crop/index、空マス、収穫済み、一時停止、1/2/4倍速、予測と実更新の一致、プレビューの本体非変更をテスト。新しい永続値を追加する段階では旧セーブ、Undo、Timelineの往復も必須。大きな時間刻みで適量区間を飛び越える問題と警告のちらつきに注意する。
- 性能・DX12：作物ごとの判定は定数時間、全体はO(マス数)。毎フレームの動的確保・Texture生成は避ける。今回のルール追加はBarrier/Fence/Descriptor/VRAM所有権を変更する理由にはしない。既存地形のDrawCall・メモリ負荷はマス拡張前の別計測対象。

途中の案の扱い：読みづらいHUD、操作不能、保存・データ破損は現在作業へ優先して取り込む。新しい遊びは「目的・変更箇所・代わりに後回しにする作業」を確認し、この仕様の計画へ反映する。大きな案を同時に積まず、各段階で「ゲーム操作＋HUD＋デバッグ＋テスト＋短いプレイ確認」まで一区切りにする。設計を更新したことと実装が完了したことは区別する。

## 実装済み・検証中

- 5×4の論理農地グリッドと選択マス操作
- タイルごとの高さレベル H0〜H2
- Hoe / Water / Seed / Harvest による栽培の基本ループ
- カブとニンジンの種購入、成長、収穫、品質、販売
- 水分による成長速度の変化と成長予測表示
- Undo / Redo、FarmドキュメントのSave / Load
- 用水路基盤の第1段階：未使用マスへの用水路設置・解除、Undo / Redo、Save / Load、論理・HUD・Inspector・簡易描画の同期
- 給水判定の第2段階：未使用マスへの水源設置・解除、水源から同じ高さまたは低い用水路への4近傍通水、乾いた用水路と通水中の用水路の表示分離
- 灌漑効果の第3段階：通水中の用水路に4近傍で隣接し、用水路と同じ高さまたは低い耕地の水分を継続回復。通水方向、灌漑対象、回復量、水分収支をゲーム画面・HUD・Farm Map・Farm Inspectorで確認可能
- 供給強度の第4段階：水源を100%として、用水路を1マス進むたびに伝達効率で減衰し、分岐では下流本数へ均等分配。畑は隣接する中で最も強い用水路から給水され、強度に比例して水分を回復
- 灌漑プレビューの第1段階：選択した1マスの用水路または水源の設置・撤去を一時Gridで仮計算し、流れ・供給強度・灌漑範囲を3D表示、HUD、Farm Map、Farm Inspectorで確認してから確定または取消可能
- 灌漑プレビューの第2段階：選択した1マスをH0〜H2の範囲で上げ下げした結果も一時Gridで仮計算し、高低差による通水・切断を確定前に比較可能
- 灌漑プレビューの第3段階：Farm Mapの用水路ブラシで上下左右に隣接する未使用マスをドラッグし、複数マスの経路をまとめて仮計算・確定・Undo / Redo可能
- 灌漑プレビューの第4段階：用水路ブラシを設置・撤去に切り替え、経路を引き返して候補を短縮可能。撤去による給水切断も確定前に表示
- 灌漑プレビューの第5段階：同じ行・列へ素早くドラッグした場合に途中のマスを補間。区間全体を検証してから仮反映し、通れないマスと理由を畑マップ・Inspectorへ表示
- 水量の第1段階：水源・用水路に永続的な貯水量を追加。補給、同高・低所への移送、複数耕地への有限配分、切断後の残水消費に対応。HUDの「水量」、Inspectorの「貯水量」、Mapの青バーで確認可能

## 確定した設計判断

用水路と水源は `Empty / Tilled / Planted` といった栽培状態とは別の土地機能です。そのため、タイルの `FarmTileState` に追加せず、直交する `FarmTileFeature` として管理します。栽培中のマスと土地機能の重複は禁止します。水源と用水路は保存対象ですが、通水中かどうかは地形から再計算できる派生状態なので保存しません。

## 未実装・後続段階

- 水位差・圧力による自由な流れ、切断された用水路同士の残水移送、溢水・蒸発・過湿被害、最終的な水面描画とバランス
- 直接水やりと自動灌漑の最終バランス調整
- 水面・流れの3D描画、段差メッシュ、地形の連続編集
- 用水路と水分状態が作物の品質・サイズ・成長に与える最終バランス
- 30日目の巨大作物コンテストと最終リザルト

---

# 1. このゲームを一言で言うと

**高さを自由に変えられる3Dの農地で用水路を設計し、水の流れと溜まり方をコントロールしながら作物を育て、30日目の巨大作物コンテスト優勝を目指す農業サンドボックスゲーム。**

---

# 2. このゲームで一番重要な特徴

このゲームは、

「畑を耕して、水をやって、作物を収穫するだけ」

の農業ゲームにはしません。

ゲームの中心は、

**Terrain × Irrigation × Water × Crop**

です。

プレイヤーが、

土地を作る
↓
高低差を考える
↓
用水路を作る
↓
水を流す
↓
水が流れる・溜まる
↓
土の水分状態が変わる
↓
野菜の育ち方が変わる
↓
品質・サイズ・成長速度に影響する

という一連の仕組みを扱います。

---

# 3. このゲーム独自の重要システム

特に重要なのは以下の5つです。

1. 高さを変更できるGrid Terrain
2. 高低差を利用した用水路
3. Gridベースの水流シミュレーション
4. 水量・水流・水溜まりの可視化
5. 水による野菜の成長変化の可視化

この5つを連携させることが、この作品の技術的・ゲーム的な中心です。

---

# 4. ゲームの基本ループ

基本的な1サイクルは、

土地を選択
↓
土地の高さを変更
↓
畑を耕す
↓
用水路を作る
↓
水源から水を流す
↓
水の流れ・溜まり具合を確認
↓
種を植える
↓
水分によって作物が成長
↓
虫などの問題へ対応
↓
収穫
↓
売却
↓
農場を改善
↓
コンテストに挑戦

となります。

---

# 5. 30日制

ゲームは30日間。

基本設定：

- 1日 = 約60秒
- Time Scale = 1x / 2x / 4x
- Debug用Day Skipあり

コンテスト日：

- Day 10
- Day 20
- Day 30

最終目標：

**Day30の巨大作物コンテストで優勝する。**

総売上だけではゲームクリアになりません。

---

# 6. 30日間の大きな流れ

## Day 1～9

農業の基本を覚える期間。

- 土地
- 高さ
- 耕す
- 水
- 種
- 成長
- 収穫

などを理解する。

---

## Day 10

第1回コンテスト。

現在の基本案：

Days 1～10で収穫した作物の中から、最も評価の高い作物を自動提出する。

---

## Day 11～19

中盤。

単純な農業から、

- 水管理
- 品質
- 用水路
- 畑の高低差
- 虫

などを意識する段階へ進む。

---

## Day 20

第2回コンテスト。

Days 11～20で収穫した作物を中心に品質を評価する。

---

## Day 21～29

最終コンテストへ向けた育成。

- サイズ
- 品質
- 水管理
- 巨大化
- 農場設計

を重視する。

---

## Day 30

最終コンテスト。

**巨大作物コンテスト**

ここで勝利するとゲームクリア。

---

# 7. コンテスト

## Day 10

初心者向けコンテスト。

全対象作物について、

Days 1～10で最も良かった収穫物を自動提出する方向。

---

## Day 20

品質コンテスト。

Days 11～20で最も良かった作物を評価する方向。

---

## Day 30

巨大作物コンテスト。

Days 1～30の中から最良の作物を評価する方向。

単純な絶対サイズだけではなく、

**その作物の通常サイズに対してどれだけ巨大化したか**

を利用する。

例：

巨大化率 = 実際のサイズ ÷ 作物の基準サイズ

これによって、

- にんじん
- トマト
- かぼちゃ

を比較できるようにする。

かぼちゃが巨大化しやすい有力ルートであっても、非常によく育てた人参やトマトでも勝てる設計を目指す。

---

# 8. 作物

現在想定：

- にんじん
- トマト
- かぼちゃ

---

# 9. 作物の方向性

## にんじん

- 成長が速め
- 育てやすい
- 序盤向け
- 巨大化より回転率を重視

## トマト

- バランス型
- 品質を狙いやすい
- 水管理の影響を理解する中盤向け

## かぼちゃ

- 成長が遅め
- 大型化しやすい
- 終盤・巨大作物向け

---

# 10. 作物解放時期

現在の候補案：

- Day 1：にんじん
- Day 10前後：トマト
- Day 20前後：かぼちゃ

ただし、これは完全確定ではありません。

Codex側で勝手にハードコードしないでください。

JSONやConfig化が適切になった段階で調整可能にします。

---

# 11. 巨大かぼちゃ 2×2案

検討中の特徴。

通常：

1×1

で植える。

一定条件を満たして巨大化すると、

2×2

を使用する巨大かぼちゃになる案。

条件候補：

- 十分に成長
- 適切な水分
- 十分な品質
- 周囲3マスが使用可能
- 高さ条件を満たしている

これはまだ完全確定ではありません。

最初からFarmGridを2×2作物前提で複雑化しないでください。

---

# 12. FarmGrid

農場はGridベース。

1Tile = 農場の1マス。

FarmTileが持つ基本情報：

- heightLevel
- state
- crop
- moisture
- growth

など。

既存実装を必ず確認すること。

---

# 13. 土地の高さ

## プレイヤーの接地・水路の通行（2026-09-05実装）

土は描画と同じ三角形、水路と水源は描画と同じ底・岸の上面を足場として使う。水面は足場にしない。土・水路底は足元中心、岸はPlayerのAABBのXZ範囲との重なりで判定する。畝の小さな凹凸は歩行面に含めない。

- 開発モードで畑を選択し、高さや用水路のプレビューを確定する。接地は確定済みの畑だけを参照する。
- ゲームビューを選択し、WASDで移動、Spaceでジャンプ。低い岸は乗り越え、高い岸で止まる場合はジャンプで岸より高く上がって越える。接続された水路の間には岸の判定を追加しない。
- 小さな下り坂には追従し、大きな下り段差は落下する。空中では段差への自動乗り上げを行わない。足元で地形を上げた場合は埋まりを解消する。
- 水量が増減しても足場の高さは変わらない。泳ぎ・浮力・水による減速は未実装。現在は浅い水路を底面に接地して歩くプロトタイプ。
- Inspectorの「プレイテスト」→「プレイヤーの接地」に、接地中／空中、XYZ、足元の地形（土／水路底／岸／地面）、地形の高さとマス番号、移動制限の有無を表示する。空中でも「足元の地形」は下方の候補を示すため、接地中の表示とは区別する。ゲームHUDは増やさない。

上りステップ上限0.25、下降追従距離0.12、横移動の分割幅0.05（いずれもworld unit）、最大分割数32はLevelGameplaySystemのconstexpr。通常のFixedUpdateでは1分割。異常に長い更新では横移動距離を制限して処理量を抑える。最大登坂角度、カプセル衝突、連続3D衝突、屋根・オーバーハングには対応していない。畑外は既存GROUNDを使い、これより低い掘り込みは未対応。

開発用の移動確認：畑マップでマスを選び、Inspectorの「プレイテスト」→「プレイヤーの接地」→「選択マスへプレイヤーを移動」を押す。選択マスの中央の足場へ移動し、プレイヤー追従カメラを同期する。その場所からWASDとSpaceで移動・ジャンプを確認できる。ゲームモード・灌漑プレビュー中・履歴の巻き戻し中・クリア後は利用できない。範囲外や障害物内への移動は拒否する。移動操作自体は畑・所持金・保存状態・農業Undoを変更せず、プレイヤー状態をTimelineへ記録する。実行中の通常の時間進行は停止しない。

責務：FarmMeshLayoutが底・岸の形状種別を付け、FarmTerrainQuerySystemがCPU形状を読み取る。LevelGameplaySystemが段差拒否・軸別移動・鉛直速度・接地・検証済み位置への移動を更新。Sceneは同期呼び出し中だけ有効な問い合わせを渡し、カメラとTimelineを同期する。Bridgeは世代・選択番号を検証し、UIは表示と移動要求だけを担当する。Save形式、灌漑ルール、描画形状、GPU所有権は変更しない。

検証：土の既存テストに加え、水路の接続16パターンで描画上面との一致、水量変更、岸への足幅の接触、高い岸での停止、岸沿いの移動、ジャンプ・着地、低い岸の乗り越え、長い更新でのすり抜け防止、撤去・履歴復元、無効入力をCPUテストで確認。配置では空中からの速度リセット、範囲外・NaN・不正な問い合わせ・障害物への配置拒否、拒否時の位置不変、畑世代の不変も確認。Debug/Development/Release x64ビルド成功。通常のデスクトップ環境で起動したDebug実画面で、H2の畑・用水路・水源へのボタン移動、足場種別と高さの表示、カメラ追従、ゲームモード往復を確認した。連続歩行・ジャンプの実際の操作感、全無効化条件の目視検証、Releaseでの今回の表示確認は未完了。既存のフォント不足・未対応文字・Scale・カメラのログは未修正。

Tileには高さがあります。

現在：

- H0
- H1
- H2

土地を上げたり下げたりできます。

PopulousのようなGridベースの地形操作をイメージしています。

この高さは見た目だけではありません。

**水流計算へ使用します。**

---

# 14. 用水路システム

このゲームでは用水路を作れるようにします。

基本構造：

水源
↓
用水路
↓
農地
↓
作物

プレイヤーが、

「どこから水を持ってくるか」

「どの高さへ水路を通すか」

「どこに水を溜めるか」

を考えます。

## 現在のプロトタイプ操作

- `N`：選択中の未使用マスに用水路を設置、または撤去
- `M`：選択中の未使用マスに水源を設置、または撤去
- 用水路と水源は栽培状態、作物、水分、成長度を持つマスには設置できない
- 設置・撤去と高さ変更は既存のUndo / Redo対象
- 水源と用水路はFarmドキュメントへ保存する

HUD、Farm Map、Farm Inspector、簡易3D線表示では、水源、未通水の用水路、通水中の用水路を区別します。通水方向は用水路上の矢印、灌漑対象は水色の接続線とマーカー、HUDの「灌漑中」、Farm Map上端の水色線で表示します。

---

# 15. 用水路と高低差

基本ルール：

**水は高い場所から低い場所へ流れる。**

例：

H2 水源
↓
H2 用水路
↓
H1 用水路
↓
H0 農地

高い場所へ自然に逆流しない。

現在のプロトタイプでは、水源を始点として上下左右の4近傍を探索します。接続先が用水路で、接続元と同じ高さまたは低い場合だけ通水します。斜め接続と高いマスへの逆流は行いません。

---

# 16. 水流シミュレーション

本格的な流体シミュレーションにはしません。

Grid単位の離散的な簡易シミュレーションとします。

イメージ：

水があるTile
↓
隣接Tile確認
↓
低いTileへ移動
↓
同じ高さなら横へ広がる
↓
低い場所へ水が集中
↓
水が溜まる

目的は物理的に完全な水ではなく、

**プレイヤーが理解・予測できる水**

です。

## 接続強度と水量の役割

現時点では、複数の水源を始点とする幅優先探索で「到達できる用水路」を求めます。探索結果は地形編集、Undo / Redo、Load、再起動後に再構築します。

水源は100%の供給強度を持ちます。用水路を1マス進むたびに設定された伝達効率を掛け、1つの親から複数の下流用水路へ分岐する場合は、強度をその本数へ均等に分配します。経路は既存の幅優先探索が確定した親子関係を使うため、同じ地形では常に同じ結果になります。

通水中の用水路に上下左右で隣接する通常マスは、そのマスが用水路より高くない場合に「灌漑範囲」となります。複数の用水路が隣接する場合は供給強度が最も高い用水路を採用し、同値なら固定された探索方向で決定します。水源へ直接隣接するだけでは灌漑されず、必ず用水路を経由します。

FixedUpdateでは `FarmIrrigationSystem` が補給・移送・土壌への給水を行い、その後 `FarmGrowthSystem` が成長・乾燥を更新します。土壌への給水は残水量で制限されるため、接続強度が正でも乾いた用水路から無限には給水されません。直接Waterツールは即時に土壌水分を与える手段として残します。

供給強度は地形から再計算する経路の指標です。貯水量とは別物であり、Inspectorで分離して表示します。畑の回復速度・収穫時刻の予測は現在条件での概算です。給水予測は残水が継続する場合の上限で、複数マスへの配分や残水切れで低下します。

---

# 17. 水量

水源・用水路Tileに `waterAmount` を持たせます。通常の畑Tileは常に0です。

`waterAmount` は流入・流出・土壌への給水によって時間変化する保存対象です。JSON schema v5で保存し、旧v1〜v4は水量0で読み込みます。新形式の欠損・型違い・範囲外・通常マスへの貯水は文書全体を拒否し、現在の畑を変更しません。

現在の範囲：

waterAmount = 0.0 ～ 1.0

意味：

- 0.0 = 水なし
- 少量
- 中程度
- 多い
- 1.0 = 最大

実際の内部値は連続値でも、表示は段階的にして構いません。

## 水量の最小ルール

1. 水源は毎秒0.5を補給し、容量1で停止します。
2. 水源に接続された用水路は既存の探索木に沿い、親の貯水から毎秒最大0.4を下流へ移します。分岐では親の移送枠を均等分割し、満杯の子は受け取りません。余った枠の再配分はしません。
3. 全移送は同じ更新前の水量を参照します。受け取った水は同じFixedUpdateでさらに下流へは移りません。流れは上流から順次満たす簡易モデルで、物理的な波速の再現ではありません。
4. 通常マスのうち耕作済みの土へ給水します。同高・低所の上下左右に限り、残水がある用水路から選びます。複数候補は経路強度最大、同値は固定方向順。水源への直接隣接だけでは給水しません。
5. 耕地の要求量は毎秒0.125×強度を上限とし、土壌水分の空き容量で制限します。同じ用水路を使う複数耕地は要求量の比で残水を分け、実際に渡した分だけ貯水を減らします。水量1と土壌水分1は現段階では同じ量として扱います。
6. 水源を外しても用水路の残水は維持し、その用水路に隣接する耕地へ給水できます。この場合の給水強度は0.9。切断後の用水路間の移送はまだ行いません。
7. 用水路・水源の撤去はそのマスの水を破棄します。高さ変更は保持します。Undoは編集時のスナップショットへ戻し、プレビュー中は農業時間・水量・成長を停止します。プレビューは編集直後の候補であり、将来の水量シミュレーションではありません。

これらの速度は `FarmRules` の検証用仮値です。Time Scaleに連動します。過湿被害・蒸発・水位差・圧力・逆流・洪水は未実装です。

## 操作と確認

HUDとInspectorの次の4状態は、実際に移送した量ではなく現在の給水条件です。判定は `FarmIrrigationSystem::GetWaterStatus` に集約します。直前の給水実績は別に計測します。

| 状態 | 意味 |
|---|---|
| 給水可能 | 水源への接続があり、対象の用水路に水がある |
| 残水あり | 接続は切れているが、対象の用水路に水が残っている |
| 補給待ち | 接続はあるが、対象の用水路にはまだ水がない |
| 水切れ | 接続がなく、残水もなくなった |

畑を選択した場合は、同高・低所の給水条件に合う隣接用水路を判定します。水源だけへの隣接・高すぎる畑などはInspectorに「給水経路なし」と表示します。未耕作でも給水条件は表示しますが、実際の給水は耕作後です。水源自身の表示は保有水と補給条件を表し、畑への直接給水を意味しません。

用水路のマップ表示は、水色＝給水可、黄系＝残水、灰色＝待機、暗色＝乾燥。短い文字と凡例を併用し、色だけに依存しません。3Dの用水路枠も同じ状態を表し、畑への給水可能経路は残水のある用水路からだけ表示します。
HUD右側の小ステータス枠は水の状態を優先し、給水経路がない場合のみ従来の土壌の乾燥状態を表示します。土壌水分の数値は常時残し、Inspectorでは両方を確認できます。状態ラベルを縦に重ねて成長率の行へはみ出させません。

- 未使用マスを選び、水源配置のプレビューを確定。同じ高さ以下へ用水路ブラシで経路を作成して確定します。
- 実行中に水源・用水路を選ぶと、ゲームHUDの「水量」とInspectorの「貯水量」が更新されます。Farm Mapの下側の青バーは、畑では土壌水分、用水路・水源では貯水量です。上側の水色線は接続強度を維持します。
- 用水路の隣の畑を耕すと、貯水を消費しながら水分が増えます。水源を撤去して確定すると残水のみで給水し、空になると止まります。
- Save後に水量が変わった状態でLoadすると保存時の貯水を復元します。実行中は直後からまた水量が変わるため、比較時は一時停止して確認します。

---

## OBJモデルへの移行方針

現段階は仮OBJ表示です。`FarmRenderer`が専用の `Resources/farm/unit_box.obj` を共有し、土台・耕した畝・水路/水源の底と岸・水面を別インスタンスとして描画します。デバッグ線と日本語HUDは残します。完成アセットではなく、形状と水位の読み取りを検証する段階です。

操作：上部の「ウィンドウ」から「シーン表示」を開き、「畑表示」の「畑の仮モデルを表示」でON/OFFを比較できます。同じ場所に描画部品数を表示します。「撮影用」プリセットでは周囲のテストモデルを隠せます。既存の用水路/水源/高さのプレビューは、確定前の候補データをモデル表示にも使用します。操作や水量計算、保存形式は変更しません。

今回の準備として、`FarmVisualSystem::GetTileVisualData` が描画方式に依存しない値を返します。Tile中心と半幅、土の濡れ量、作物の種類・成長段階・配置基準点、貯水率と水面位置を共通化します。ライン描画もこの水面位置を使用し、水量の増減で一定面積の水面が上下します。空の水路では水面を出さず、水路の枠だけ残します。高さ変更時は配置基準点も追従します。クリック判定は水面ではなく論理Tileの面を維持します。

OBJ化するときの契約：
- 土・水路・水面・作物は別の表示部品とし、水路OBJ全体を水量に合わせて拡大縮小しません。
- Tile中心を配置の基準とし、OBJ固有の原点・寸法差は描画側で補正します。仮OBJは中心原点、各軸-1〜1で、Scaleは半寸法です。水面の底位置と最大深さは `FarmVisualLayout` の調整値であり、物理的な水深を意味しません。
- 土の濡れ量は0〜1のMaterial入力として使い、乾いた土と濡れた土の見た目を変えます。通常の土壌水分から水面は作りません。
- 水量0では水面を非表示にし、水量1で最大水面位置にします。立体の水源モデル自体は水量0でも消しません。
- OBJは形状・UVを担当し、水面の動きや濡れたMaterial表現はC++側の描画処理が担当します。共有Model/Materialをマスごとの変更で上書きせず、必要な値を各インスタンスへ渡します。
- デバッグ色・経路線・選択枠は確認用オーバーレイとして残し、完成版の認識を色だけに依存させません。

地形表示：ポピュラスの参考画像から「草の上面・茶色い土の側面・高さの違いを坂でつなぐ地形」を取り入れます。画像やアセットのコピーは行いません。未耕作は草の上面と土の本体、耕作後は土色の上面と3本の畝。湿った土は暗くします。マス中央は耕作・作物配置用に平らに残し、辺は隣接2マス、四隅は接する最大4マスの論理高さの平均へつなぎます。各マスを3×3区画に分け、ねじれた区画は2枚の三角面に分割します。共有境界の座標と高さをそろえ、通常の土同士の隙間をなくします。土の本体は地下まで延ばしますが、地下側の形状や海岸は完成形ではありません。作物・灌漑の論理高さとルールは変更せず、プレイヤーの坂への接地は別途対応します。

水路表示：マス中央を平らに保ち、接続先までを傾斜した底・岸・水面でつなぎます。境界の地面高さは両マスの平均、水面高さも両マスの水面高さの平均にそろえます。旧方式の垂直な水面と、一枚の高い底ブロックは廃止します。底を9区画、側岸を3区画ずつに分け、坂を高い底や岸が遮らないようにします。通常の土の坂とは別の形状生成であり、水路の岸と土の接合部の最終調整は残っています。

双方に水がある上下左右の方向だけ水面の坂を出します。乾いた接続先にも水路の底は続きますが、水面は出しません。斜め隣や通常の土へ水面をつなぐこともありません。貯水量・地形高さ・プレビュー候補に合わせて傾斜を再計算します。これは連続した水路を見せる幾何表現であり、圧力や流速を解く水理モデルではありません。灌漑の高低差判定は論理マスの高さを維持します。

検証範囲：水路は上下左右×H0〜H2の高さ組み合わせ、異なる正の貯水量、境界での底・水面・直線側岸の高さ一致、16通りの接続と乾燥状態をCPUテストで確認。通常の土は60通りの3×3高さ配置について、共有辺・四隅の一致、描画面と選択判定の一致、部品数上限、プレビューの元データ非変更を確認しました。三角形OBJは実際のAssimp読込で頂点方向・法線・UVを確認。Debug/Development/Releaseビルド成功、Release実画面で畑の描画を確認しました。ただし、複雑な高低差や分岐を含む傾斜の目視確認は未完了です。

水面は不透明・非ライティングで、水量に応じて高さが変わり、0なら消えます。影は落としません。水量の計算・接続ルール・保存形式は変更せず、プレビュー時は候補の状態から同じ描画を組み立てます。通常の土は描画と共通の傾斜面でクリック判定し、元のマス間の隙間でも選択できます。水路・水源の選択は引き続き水面ではなく論理Tile面です。

性能と制限：部品ごとにObject3dの専用Constant Bufferを持ち、共有Modelの状態は変更しません。傾斜はインスタンスのY方向Shear（X/Z移動量に比例してYを変える変形）で作り、既存のWorld・逆転置法線・影の行列へ反映します。既定のShearは0で、既存オブジェクトは変形しません。部品は再利用し、Scene終了まで保持します。通常の土は平地で2部品、耕作後5部品、複雑な傾斜と畝を含め最大29部品です。孤立した濡れた水路は22部品、四方向接続は14部品。上限を640部品とし、超過またはOBJ不足時は既存の線へ戻します。20マスすべてが最大構成なら580部品となり、専用Constant Bufferの確保粒度を含む負荷は大きい状態です。DrawCall削減やInstancing/バッファ統合は未実施で、GPU性能は未計測です。既存のフレームFence待機に依存し、Descriptor/Barrier方式は変えていません。

未実装：完成作物モデル、土のTexture/Materialブレンド、透明な水と波のShader、土と水路の岸の接合部・分岐外側角・地下側の最終形状、遮蔽の調整、プレイヤーの斜面への接地。通常の土の上面選択には対応しましたが、水面・岸・作物の個別モデル部品単位のクリック判定は行いません。

## 直前の給水実績（計測と確認）

畑マップ上部の「作物表示／水分表示」で表示内容を切り替えられます。水分表示はマス番号・土壌水分または貯水率・短い給水条件を表示します。畑の色は茶色（乾燥）から青色（多水分）へ変わり、水路は給水可／残水／待機／乾燥の区別を維持します。直前に受水した畑には水色の点が付き、カーソルを重ねると受水実績を確認できます。一時停止中の点は履歴で、プレビュー中・無効な計測では表示しません。狭いウィンドウでは横スクロールを使用します。表示を切り替えても畑データは変えません。

`FarmIrrigationSystem` は最後のFixedUpdateについて、補給・水路間の移送・畑への実際の給水を記録します。単位は水路1マスの満水量＝1で、リットルや毎秒量ではありません。1描画フレーム内に複数回更新した場合も最後の1回のみです。

- Inspectorの「直前の給水実績」を開くと、計測前後の貯水・水源補給・水路内移送・畑への給水・水収支の誤差を確認できます。
- 水収支は「計測前の貯水＋水源補給－畑への給水－計測後の貯水」。水路内移送は内部の移動なので加減算しません。浮動小数点の丸め誤差は残ります。
- マスを選択すると、そのマスの補給・水路流入/流出・畑受水・隣の畑への給水を確認できます。
- HUDは選択中の畑が最後の更新で実際に受水した場合のみ「直前給水」を既存の状態枠に表示します。それ以外は給水条件を表示します。土壌水分の数値は維持します。
- 一時停止中は最後の実績を保持します。現在も給水中という意味ではありません。通常の高さ・設備・作付け状態の変更、Grid再初期化/Load等による世代変更では無効化します。プレビューには実績を表示しません。単純な水分値の直接変更は履歴を消さず、計測は保存データにも含めません。

確認手順：水源→同じ高さ以下の用水路→隣接する耕作済みマスを作成・確定し、時間を進めます。畑を選択して「直前給水」とInspectorの受水量を確認し、一時停止して水収支を比較します。水源を撤去後に再開し、残水の消費と受水量が0になる様子を確認します。成長処理による水分減衰は給水後なので、最終水分の増減と給水実績は一致しないことがあります。

---

# 18. moistureとの違い

現在は、

## waterAmount

その場所に存在する水そのもの。

## moisture

土に染み込んでいる水分。

として分離するのが自然です。

例：

用水路に水が流れる
↓
隣接する畑へ水が供給される
↓
土のmoistureが上がる
↓
作物の成長へ影響する

ただし、最初から複雑化しすぎないこと。

---

# 19. 水が溜まる仕組み

低いTileや流れの終端では、水が溜まるようにします。

例：

H2
↓
H1
↓
H0

H0で出口がない場合、

水が蓄積。

これによって、

「水を流せば必ず良い」

わけではなくなります。

---

# 20. 水量の可視化

重要機能。

水があるかだけではなく、

**どの程度水が溜まっているかを色で表示する。**

例：

- 水なし：通常表示
- 少量：薄い水色
- 中程度：水色
- 多い：青
- 最大：濃い青

目的：

数字を読まなくても、

- 水が来ている
- 水が足りない
- 水が多い
- 水が溜まりすぎている

ことが分かるようにする。

---

# 21. 水流方向の可視化

色だけではなく、流れる方向も表示できるようにします。

例：

→
←
↑
↓

または3D上のArrow。

可視化候補：

- Flow Arrow
- Water Amount
- Water Color
- Tile Height
- Flow / Pool / Dry状態

---

# 22. NEW：水による作物成長変化

水システムは作物育成と接続します。

流れ：

用水路
↓
水が農地へ届く
↓
土のmoistureが変化
↓
作物の状態が変化

水が適量：

- 正常成長
- 成長速度向上
- 品質を維持しやすい

水不足：

- 成長低下
- 品質低下の可能性

水過多：

- 成長効率低下
- 品質低下などのペナルティ候補

重要：

「水が多ければ多いほど良い」

という単純な仕様にしない方向。

**適切な水量を維持することに意味を持たせる。**

具体的な数値はまだ確定しないでください。

---

# 23. NEW：作物への影響可視化

用水路による水の状態だけではなく、

**その水の状態によって作物がどのように育つのかも視覚的に確認できるようにする。**

これをこのゲームの重要な特徴に追加します。

---

# 24. Crop Growth Visualization

作物の状態を数字だけで表示しない。

プレイヤーが一目で、

「この場所ならよく育つ」

「ここは水不足」

「ここは水が多すぎる」

と判断できるようにする。

表示例：

## 良好

緑系表示

状態：

「成長良好」

## 水不足

黄～橙系

状態：

「水不足」

## 水過多

別の警告表現

状態：

「水過多」

## 成長停止・危険

赤系警告

状態：

「成長低下」

ただし、水量表示の青と作物影響表示を同時に重ねて分かりにくくしないこと。

---

# 25. Water ViewとCrop Impact View

可視化Modeを分ける案を推奨。

## Water View

表示：

- 水量
- 水流
- 水溜まり
- 水の到達範囲

## Crop Impact View

表示：

- 水が各作物へ与える影響
- 成長速度
- 水不足
- 適正
- 水過多

これにより、

青色 = 水

緑 / 黄 / 赤 = 作物への影響

と役割を分けられます。

---

# 26. 作物の成長そのものも見た目で変化

作物モデルも成長段階に応じて変化させます。

例：

Stage 0
種

↓

Stage 1
芽

↓

Stage 2
小さい作物

↓

Stage 3
成長中

↓

Stage 4
収穫可能

さらに、

水不足の場合は成長が遅い。

適正水量なら正常。

条件が良い場合は、

- サイズ
- 品質

が伸びる。

こうすることで、

用水路を変更した結果が実際の作物の見た目にも現れます。

---

# 27. 灌漑プレビュー

このゲームの重要なポートフォリオ機能。

プレイヤーが、

- 地形
- 用水路
- 水源

を変更する前に、

**変更後の水の流れを事前シミュレーションできるようにする。**

## 現在の第1〜第5段階実装

Farm Inspectorで、選択中の1マスに対する用水路または水源の設置・撤去をプレビューできます。開始時に現在のFarmGridを一時Gridへコピーし、候補変更は一時Gridだけに適用します。仮の水源数、通水用水路数、灌漑範囲、各マスの供給強度と成長予測を再計算し、変更対象を橙色で明示します。

高さのマイナス・プラス操作も同じプレビューへ統合します。選択マスをH0〜H2の範囲で1段階上げ下げし、その結果として用水路が高所で切断されるか、低所へ通水できるかを3D表示、HUD、Farm Map、Farm Inspectorへ即時反映します。Farm Inspectorには `H1 -> H2` のように変更前後を表示します。

Farm Mapの「用水路ブラシ」を有効にすると、最初の未使用マスを押してプレビューを開始し、左ボタンを押したまま上下左右へ移動して用水路候補を延長できます。同じ行または列なら途中のマスも補間します。斜めの終点には経路を推測せず、曲がり角で一度止めてから方向を変えます。区間内に耕作済みマス、水源、既存用水路、過去の候補との交差などがあれば区間全体を追加せず、通れないマス番号を表示します。範囲外も拒否します。ドラッグを離しても候補は保持し、すべての候補を橙色で表示するとともに、Farm InspectorとHUDへ候補数を表示します。

「この変更を確定」を押した場合だけ既存の `FarmToolActionSystem` を通して本Gridへ反映するため、Undo / Redoと未保存管理の対象になります。複数マスの用水路候補は1つのCommandとして原子的に確定し、途中の適用に失敗した場合は適用済みマスも元へ戻します。そのため1回のUndo / Redoで経路全体が戻る・再適用されます。「プレビューを取消」では本Gridを変更しません。プレビュー開始後に本Gridの内容が変化した場合、古い予測の確定を拒否します。

ブラシの「設置」「撤去」は候補がないときに切り替えます。撤去では既存の用水路だけを対象とし、水源や作物は削除しません。設置・撤去とも、経路上の以前のマスへ引き返すと、そのマスまでを残して後続候補を取り消します。先頭マスは残り、全候補を破棄するには「プレビューを取消」を使います。撤去も1回のUndo / Redoで経路全体を復元・再撤去できます。HUDは「用水路撤去予測中」、Inspectorは撤去候補数を表示します。

プレビュー中は農地の成長・水分更新と日付を一時停止し、確定・取消後に再開します。これにより操作中の自然成長で予測が古くなることを防ぎます。Load等で同じ内容の農地へ置き換わった場合も、世代番号の違いにより古い候補の確定を拒否します。

撤去でも同じ行・列の途中マスを補間し、区間内の全マスが既存用水路であることを検証してから仮反映します。途中に空きマスや水源があれば、区間全体を追加しません。拒否してもそれ以前の候補は保持されるので、方向を変えて続けられます。

障害物の自動迂回、斜め終点への自動折れ線経路、1操作内での分岐経路作成、作物品質・最終収量の比較表示は後続段階とします。

---

# 28. 灌漑プレビューの内部構造

実FarmGridを直接変更しない。

現在FarmGrid
↓
Temporary Gridへコピー
↓
仮の地形・水路変更
↓
Temporary GridでWater Simulation
↓
結果を可視化
↓
Confirm
↓
本Gridへ反映

Cancelした場合、

本物のFarmGridは変更しない。

現在の実装では、一時Grid、候補経路、追加条件、再計算を `FarmIrrigationPreviewSystem` が持ちます。確定時の全マス事前検証と原子的な一括適用は `FarmToolActionSystem` が持ちます。UIは開始・延長・確定・取消のCommandを通知するだけで、一時Gridや本Gridを直接変更しません。

---

# 29. NEW：灌漑プレビューで作物への影響も予測

灌漑プレビューを水だけで終わらせない。

新しい流れ：

用水路を仮変更
↓
Temporary Gridで水流計算
↓
各Tileの予想水量を計算
↓
農地の予想moistureを計算
↓
現在植えられている作物への影響を評価
↓
画面へ表示

つまり、

**「この用水路を作ったら、水がこう流れて、この野菜はこう育つ」**

まで確認できるようにします。

---

# 30. Crop Impact Preview

Previewでは実際の作物データを変更しません。

表示するのは予測結果。

例：

トマトA

現在：
成長 45%

変更後予測：
水分 適正
成長効率 ↑

にんじんB

現在：
成長 70%

変更後予測：
水不足
成長効率 ↓

かぼちゃC

現在：
水量過多
品質低下リスク

のように表示できる方向。

ただし、大量の文章を常時表示しない。

---

# 31. Crop Impact Previewの見せ方

優先：

## 色

- 良好
- 注意
- 悪い

## アイコン

例：

↑
→
↓

## 短い日本語

- 良好
- 水不足
- 水過多

詳細数値はTileを選択した時だけInspector/HUDに表示。

---

# 32. この可視化の目的

これは単なるDebug機能ではありません。

プレイヤーが、

「なぜこの野菜が育たないのか」

を理解するためのゲーム機能です。

また会社提出時には、

地形
↓
水流
↓
土壌
↓
作物

という複数Systemが連携していることを視覚的に見せられます。

---

# 33. 虫システム

虫はDay6頃から出現する方向。

通常：

作物Tileごとに約5%の発生候補。

虫除け植物などの効果範囲：

約1%候補。

範囲：

Manhattan Distance 2候補。

虫を放置すると隣接作物へ広がる。

BugNetで除去。

---

# 34. 虫ダメージ

Day Endで処理。

以前から存在する虫：

作物品質へ悪影響。

その日に新しく発生した虫：

その日は警告。

翌日からダメージ。

虫被害数は最終評価にも使用可能。

---

# 35. Farm Rank

Farm Rankを設定。

現在の最終目標候補：

Rank 4。

Rank評価候補：

- 総売上
- 収穫数
- 品質
- サイズ

---

# 36. 畑拡張

FarmGridを毎回Resizeする方式は避けます。

推奨：

最大Gridを最初から保持。

各Tileに、

Locked / Unlocked

などの状態を持たせ、

ゲーム進行によって使用可能範囲を広げる。

これなら、

- Tile Index
- Water Flow
- Selection
- Save Data

を壊しにくい。

具体的な最終Gridサイズは未確定。

---

# 37. Tool

現在：

- Hoe
- Water
- Seed
- Harvest
- BugNet

さらに、

- Terrain操作
- Canal作成

をゲームUIへ統合する予定。

---

# 38. プレイヤーUI

Release版ゲームはImGuiに依存させない。

ImGuiはDebug / Editor専用。

プレイヤーUIは日本語。

表示候補：

- Day
- Money
- Farm Rank
- Tool
- Seed
- Time

選択中作物：

- 品質
- 水量
- 成長
- サイズ
- 虫
- 収穫可能

---

# 39. 可視化UI

ゲーム内で必要に応じて、

Normal
Water
Crop Impact

などを切り替える方向。

## Normal

通常ゲーム表示。

## Water

- 水量
- 水の流れ
- 溜まり

## Crop Impact

- 水が作物へ与える影響
- 良好
- 不足
- 過多

Debug文字を画面いっぱいに表示する方式は避けます。

---

# 40. 最終評価

Day30終了後：

S / A / B / C / D

などで総合評価。

候補：

- 総売上
- Farm Rank
- 最高品質
- 最大サイズ
- コンテスト結果
- 虫被害数

---

# 41. 現在存在する主なFarm実装

現在把握しているもの：

## FarmGrid

- Grid管理
- Tile選択
- heightLevel
- H0/H1/H2
- Selected Tile

## FarmDateSystem

- Day
- 1x / 2x / 4x
- Day Advance

## FarmToolSystem

- Hoe
- Water
- Seed
- Harvest
- BugNet

## FarmToolActionSystem

現在主に：

- Hoe
- Water
- Seed

## FarmHUD

- Day
- Money
- Rank
- Tool
- TimeScale
- Selected Tile Info

## Farm Debug

Tile状態などをDebug確認可能。

実装前には必ず現在コードで再確認してください。

---

# 42. 今後必要な主要Game System

優先候補：

- FarmGridRenderer
- CropSystem
- Crop Growth
- Harvest
- Crop Renderer / Model
- Money / Sell
- FarmWaterFlowSystem
- Canal System
- Water Visualization
- Soil Moisture連携
- Crop Water Response
- Crop Impact Visualization
- Irrigation Preview
- Crop Impact Preview
- BugSystem
- ContestSystem
- FarmRank
- ResultSystem

---

# 43. System間の重要なデータフロー

このゲームで最も重要。

Terrain Height
↓
Canal System
↓
Water Flow System
↓
waterAmount
↓
Soil moisture
↓
Crop System
↓
Growth / Quality / Size
↓
Contest

可視化は、

Water Flow System
↓
Water Visualization

と、

Crop System
↓
Crop Impact Visualization

を担当する。

---

# 44. Engine側とGame側の分離

## Engine

project/engine

ゲーム非依存。

例：

- DirectX12
- Rendering
- Sprite
- Object3d
- Model
- Input
- Animation
- Skinning
- JSON
- Generic Debug Infrastructure

---

## Game

project/application/farm

農業ゲーム専用。

例：

- FarmGrid
- FarmTile
- FarmDateSystem
- FarmToolSystem
- FarmToolActionSystem
- FarmWaterFlowSystem
- FarmCanalSystem
- CropSystem
- CropImpactSystem
- BugSystem
- ContestSystem
- FarmHUD

**Engineからapplication/farmを参照してはいけません。**

---

# 45. Game.cpp

Game.cppへFarm固有ロジックを入れない。

Game.cppは、

- アプリ起動
- Scene
- Engineの共通セットアップ

程度にする。

---

# 46. GamePlayScene

GamePlaySceneは各Systemをつなぐ指揮役。

ここへ、

水流計算
作物成長計算
虫計算
Contest計算

などを直接大量に書かない。

Systemへ分割する。

---

# 47. 自作Editor

## 農業専用の画面構成（2026-09-05実装）

以下を現在の構成とし、下記の汎用モード一覧は将来案として扱う。

- 上段の「畑エディター」「表示」「ウィンドウ」は維持し、言語・フォント設定の入口を残す。その下の固定ツールバーに「開発モード」「ゲームモード」を置く。
- 開発モード：中央にゲームビュー、左に畑マップ、右に畑インスペクター、下にコンソール・操作履歴。PostEffect等の汎用画面は標準では開かず、必要なときだけ既存メニューから開く。
- ゲームモード：開発パネルを隠してプレイヤーカメラのゲームビューを拡大する。日本語HUD、作物の仮表示、選択枠は残し、診断用グリッド・Collider等を隠す。
- 新規作成・セーブ一覧・保存・名前を付けて保存・一時停止は両モードで使える。Undo/Redo、ステップ実行、レイアウト初期化は開発モードにまとめる。
- HUDは既存のSprite描画、ImGuiは開発画面とツールバーに使用する。Releaseでも農業HUDを初期化する。

操作は「ゲームモード」を選択して畑を操作し、調整時に「開発モード」へ戻す。元のドック配置・カメラ設定を復元し、選択マスを引き継ぐ。灌漑プレビュー中は先に確定またはキャンセルする。履歴スクラブ中の切り替えも拒否する。

畑・所持金・進行状態・一時停止状態は共有する。モード切り替えでリセット・自動保存は行わず、UnityのPlay終了時の変更破棄とは異なる。ビルド構成のDebug/Development/Releaseとも別の概念である。

起動レベルは`Resources/levels/farm_scene.json`とし、プレイヤー・地面・カメラ・ライトを読み込む。畑は既存Farm描画が生成する。巨大な金色の収集物・旧GoalTriggerは含めず、元の`scene.json`とアセットは残す。旧CG5デモと旧Field HUDは通常起動では使用しない。LevelLoader互換性のためJSONルートの`name`は`scene`とする。

検証：全3構成のビルド、実LevelLoader、Dear ImGuiの切り替え・保存要求・狭いツールバー・ドック保持テストが成功。Debug実画面でゲームモード→畑選択→開発モードの復帰を確認した。最終Release HUD表示、全農業操作、保存ファイル往復は今回の目視検証範囲外。既存のフォント・文字・ゼロ近傍Scale警告は残る。

参考：[Godotのエディター構成](https://docs.godotengine.org/en/stable/getting_started/introduction/first_look_at_the_editor.html)、[UnityのGame View](https://docs.unity3d.com/6000.0/Documentation/Manual/GameView.html)。必要な開発パネルだけを表示し、同じ畑をそのまま遊んで確認する構成として採用する。

## 将来の汎用モード案（現在の標準画面には含めない）

## 農業専用の画面構成（2026-09-05実装）

上段の「畑エディター」「表示」「ウィンドウ」は維持し、言語・フォントなどの設定入口を残す。その下の固定ツールバーに「開発モード」「ゲームモード」を置く。

- 開発モード：中央にゲームビュー、左に畑マップ、右に畑インスペクター、下にコンソール・操作履歴。農業に直接必要な情報を標準表示する。
- ゲームモード：開発パネルを隠し、プレイヤーカメラのゲームビューを拡大する。日本語HUD、作物の仮表示、選択枠は残し、診断用グリッド・Collider等は隠す。
- 両モードで新規作成・セーブ一覧・保存・名前を付けて保存と一時停止を使える。Undo/Redo、ステップ実行、レイアウト初期化は開発モードにまとめる。
- PostEffectなどの汎用調整画面は標準では開かない。必要な場合のみ既存の「ウィンドウ」から開く。
- HUDは既存のSprite描画を使用し、ImGuiは開発画面とツールバーに使用する。Releaseでも農業HUDを初期化する。

## 切り替えの操作と状態

1. ツールバーの「ゲームモード」を選び、拡大されたゲームビューで畑を選択・操作する。
2. 「開発モード」へ戻すと、元のドック配置とカメラ設定に戻る。選択した畑マスも引き継ぐ。
3. 灌漑プレビュー中は先に確定またはキャンセルする。履歴スクラブ中の切り替えも拒否する。

これは表示モードの変更であり、別の畑や保存データを生成しない。畑、所持金、進行状態、一時停止状態を共有し、切り替えによるリセット・自動保存は行わない。UnityのPlay終了時の変更破棄とは異なる。Debug/Development/Releaseというビルド構成とも別の概念である。

## 農業専用の起動シーン

`Resources/levels/farm_scene.json`を使い、プレイヤー・地面・カメラ・ライトだけをレベルから読み込む。畑は既存Farm描画側が生成する。巨大な金色の収集オブジェクトや旧GoalTriggerは農業シーンに含めない。元の`scene.json`やアセットは削除せず残す。旧CG5デモと旧Field HUDは通常の農業起動では使用しない。

現在のLevelLoaderとの互換性のため、JSONルートの`name`はファイル名ではなく`scene`とする。

## 検証範囲と参考

Debug/Development/Release x64のビルド、実LevelLoaderによる読み込み、Dear ImGuiの切り替え・保存要求・狭いツールバー・ドック保持テストを確認。Debug実画面でも「ゲームモード→畑選択→開発モード」の復帰を確認した。Releaseの最終HUD表示、全農業操作、保存ファイルの往復は今回の目視検証範囲外。既存のフォント・文字・ゼロ近傍Scale警告は残る。

設計の参考は[Godotのエディター構成](https://docs.godotengine.org/en/stable/getting_started/introduction/first_look_at_the_editor.html)と[UnityのGame View](https://docs.unity3d.com/6000.0/Documentation/Manual/GameView.html)。本作では必要な開発パネルだけを表示し、同じ畑をそのまま遊んで確認する構成として採用する。

---

# 48. Object選択

Game View内のObjectを選択。

選択Object：

黄色～オレンジ系で強調。

右Inspector：

選択Object情報を表示。

Unity / Unreal / Blenderを参考にするが、

本格Editorを作ること自体を目的にしない。

---

# 49. MVP

最低限遊べる状態：

土地を操作
↓
耕す
↓
水を供給
↓
種を植える
↓
成長
↓
収穫
↓
売却
↓
日付進行
↓
コンテスト
↓
Day30結果

まずこれを完成させる。

---

# 50. この作品で特に見せたい部分

会社提出時に特に見せたいのは、

**高さの違う土地に用水路を作り、水が実際に流れ、溜まり、その結果として野菜の育ち方まで変化する**

こと。

さらに、

**用水路を確定する前に、その水の流れと野菜への影響をPreviewできる**

という仕組みを作品の特徴として見せる。

---

# 51. ポートフォリオ上の技術的な見せ場

- 自作DirectX12 Engine
- Grid Terrain
- Terrain Height
- Canal / Irrigation System
- Grid Water Simulation
- Water Accumulation
- Water Visualization
- Soil Moisture
- Crop Growth Simulation
- Crop Impact Visualization
- Irrigation Preview
- Crop Impact Preview
- Animation / Skinning
- Debug Editor
- JSON Config
- Debug / Release Separation

ただし、

「技術を作ったからゲームへ入れる」

のではなく、

「ゲームに必要だからその技術を作る」

という順番を守る。

---

# 52. 実装優先順位

大きな順番としては、

1. FarmGridを3Dで確認できる状態
2. Hoe / Water / Seed
3. CropSystem
4. Crop Growth
5. Harvest
6. Money / Sell
7. Water Flow
8. Canal / Irrigation
9. Soil Moistureとの接続
10. Water Visualization
11. Water → Crop Growth接続
12. Crop Impact Visualization
13. Irrigation Preview
14. Crop Impact Preview
15. Bug
16. Contest
17. Farm Rank
18. Final Result
19. 演出強化

既存実装を調査し、完成済み項目を再実装しないこと。

---

# 53. 今作らないもの

現時点で不要：

- 本格Fluid Simulation
- Navier-Stokes
- 高度な水面Physics
- Unreal規模Editor
- 完全Physics Picking
- 巨大Asset Browser
- Undo / Redo
- 複雑なNPC AI
- Animation StateMachine
- IK
- Retargeting
- 高度Weather Simulation

---

# 54. 現在の仕様確定度

## 比較的確定

- 3D農業サンドボックス
- 30日制
- 1日約60秒
- 1x / 2x / 4x
- Day10 / 20 / 30 Contest
- Day30巨大作物Contest
- にんじん / トマト / かぼちゃ
- Grid Terrain
- H0/H1/H2
- 用水路
- 高低差による水流
- Gridベースの簡易水Simulation
- 水が低所へ溜まる
- 水量可視化
- 水流方向可視化
- 水が作物成長へ影響
- 作物への影響可視化
- Irrigation Preview
- Irrigation Preview上でCropへの影響も予測
- 虫
- Farm Rank
- 最終評価

## 調整可能

- FarmGrid最終サイズ
- Tile Unlock範囲
- Crop解放日
- 成長速度
- 売値
- 水量の正確な単位
- 水不足/適量/水過多の閾値
- 水によるGrowth倍率
- Quality計算
- 2×2巨大かぼちゃ
- 台風
- 宇宙人
- Contest詳細計算式

Codex側でこれらを勝手に確定しないこと。

---

# 55. Codexが必ず守ること

新しい機能を実装する前に、

1. リポジトリを調査
2. 既存Systemを確認
3. 同じ機能が存在しないか確認
4. EngineかGameか判断
5. Game.cppを肥大化させない
6. GamePlaySceneを肥大化させない
7. Magic Numberを増やさない
8. Debug / Release双方を考える
9. 必要以上に大きなSystemを作らない
10. 1機能ずつ実装する

---

# 56. このゲームを理解するうえで最重要の一文

このゲームは、

**「土地の高低差を利用して用水路を設計し、水の流れ・溜まり方を操作し、その水によって野菜の成長・品質・サイズが変化する様子まで視覚的に理解できる3D農業ゲーム」**

です。

さらに、

**「用水路を変更する前に、水流だけでなく野菜への影響まで事前シミュレーションできる」**

ことを、この作品独自の強い特徴として扱ってください。

---

# 57. Codexへの作業時の最終指示

この文書はゲーム全体の企画・方向性です。

実装依頼を受けた場合、いきなりコードを書かずに最初に、

- 今回何を作るのか
- それがゲームのどの部分なのか
- Engine側かGame側か
- 現在何が実装済みか
- 必要なClass
- Data Flow
- 既存コードへの影響
- 今回やらないこと
- オーバーエンジニアリングになっていないか

を整理してください。

目的は、

**高機能な自作Engineを完成させることではありません。**

最終目的は、

**ゲーム会社へ提出できる完成度の高い3D農業ゲームを、自作DirectX12 Engine上で完成させること**

です。
